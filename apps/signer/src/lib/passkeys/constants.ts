/** Represents the "scope" of websites on which a credential should be usable. */
export const RP_ID = "localhost";

/** The Origin represents the expected URL from which regiration or authentication occurs. */
export const EXPECTED_ORIGIN = ["http://localhost:4321"];

export const TEN_MINUTES_IN_MS = 60_000;
